15. SELECT FIRST_NAME, LAST_NAME, NAME
FROM AUTHOR, COUNTRY
WHERE NATIONALITY = COUNTRY_ID

16. SELECT TITLE, YEAR, GENRE.GENRE
FROM BOOK, GENRE
WHERE BOOK.GENRE = GENRE_ID

17. SELECT AUTHOR_ID, COUNT(*)
FROM AUTHOR, BOOK
WHERE AUTHOR = AUTHOR_ID
GROUP BY AUTHOR
HAVING COUNT(*) > 1

18.SELECT PUBL_ID, COUNT(*)
FROM PUBLISHER, BOOK
WHERE PUBLISHER = PUBL_ID
GROUP BY PUBLISHER
HAVING COUNT(*) > 3

19. SELECT TITLE, GENRE.GENRE
FROM BOOK, GENRE
WHERE GENRE.GENRE = 'POPULAR SCIENCE'

20. SELECT AVG(SOLD), LAST_NAME
FROM BOOK, AUTHOR
WHERE AUTHOR = AUTHOR_ID AND LAST_NAME = 'ROWLING'